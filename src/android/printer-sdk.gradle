// Xprinter POS SDK (TCP/LAN). Cordova applies this via plugin.xml `gradleReference`.
repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation(name: 'printer-lib-3.2.0', ext: 'aar')
}
